<ul class="nav oj-menu" role="menu">
    {{#foreach navigation}}
        <li class="{{link_class for=(url) class=(concat "nav-" slug)}}" role="menuitem"><a href="{{url absolute="true"}}">{{t "{name}" name=label}}</a></li>
    {{/foreach}}
</ul>
